//Finding factorial using a different logic
#include<stdio.h>
int main()
{
    int n,i=1,mul=1;
    printf("Enter the range of factorial: ");
    scanf("%d",&n);
    while(i<=n)
    {
        mul*=i;
        i++;
    }
    printf("\nThe factorial is: %d",mul);
    return 0;

    }
